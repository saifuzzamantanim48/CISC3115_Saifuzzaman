public static int m1(int[] arr){
        int sum = 0;

        for(int i = 0; i < arr.length; i++){
            if ((i < arr[i]) && (arr[i]%9 ==0)){
                sum += arr[i];
            }
        }
        return sum;
    }
}